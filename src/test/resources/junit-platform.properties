# instead of using per_method and having companion class in order to have beforeAll and afterAll methods, we can use
# per_class, and we're allowed to use instance method instead of static (in compation)
junit.jupiter.testinstance.lifecycle.default=per_class
